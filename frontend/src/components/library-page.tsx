import { useEffect, useState } from 'react';
import { FileUp, LoaderCircle, Search, Trash2 } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { request } from '@/src/hooks/client';

type Asset = { id: string; name: string; mimeType: string; sizeBytes: number; source: string; createdAt: string; url: string };
type Memory = { id: string; chatTitle: string; role: string; content: string; createdAt: string };
const size = (value: number) => value < 1024 * 1024 ? `${Math.ceil(value / 1024)} KB` : `${(value / 1024 / 1024).toFixed(1)} MB`;

export function LibraryPage() {
  const [tab, setTab] = useState<'files' | 'memory'>('files'); const [items, setItems] = useState<Asset[]>([]); const [memories, setMemories] = useState<Memory[]>([]); const [query, setQuery] = useState(''); const [loading, setLoading] = useState(true);
  const load = async () => { setLoading(true); try { const [assets, memory] = await Promise.all([request<Asset[]>({ url: '/library/assets', params: { query } }), request<Memory[]>({ url: '/memories', params: { query } })]); setItems(assets); setMemories(memory); } finally { setLoading(false); } };
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { void load(); }, [query]); // eslint-disable-line react-hooks/exhaustive-deps
  const upload = async (files: FileList | null) => { if (!files?.length) return; const data = new FormData(); [...files].forEach((file) => data.append('files', file)); await request<Asset[]>({ url: '/library/assets', method: 'POST', data }); await load(); };
  return <div className="mx-auto w-full max-w-6xl px-4 py-7 sm:px-8 lg:px-12"><header className="flex flex-col gap-4 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-muted-foreground">Kho cá nhân</p><h1 className="text-3xl font-semibold tracking-tight">Thư viện</h1><p className="mt-1 text-sm text-muted-foreground">Quản lý file upload, file AI tạo và memory của bạn.</p></div><label><input className="sr-only" type="file" multiple accept=".pdf,.docx,.xlsx,.pptx,.md,.csv,.json,.txt,image/*" onChange={(e) => void upload(e.target.files)} /><span className="inline-flex cursor-pointer"><Button><FileUp /> Tải file lên</Button></span></label></header><div className="mt-6 flex flex-col gap-3 sm:flex-row"><div className="flex gap-1 rounded-lg border p-1"><Button size="sm" variant={tab === 'files' ? 'secondary' : 'ghost'} onClick={() => setTab('files')}>Tệp</Button><Button size="sm" variant={tab === 'memory' ? 'secondary' : 'ghost'} onClick={() => setTab('memory')}>Memory</Button></div><label className="flex flex-1 items-center gap-2 rounded-lg border px-3"><Search size={16} className="text-muted-foreground"/><input className="w-full bg-transparent py-2 text-sm outline-none" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Tìm trong Thư viện..."/></label></div>{loading ? <p className="mt-8 flex items-center gap-2 text-sm text-muted-foreground"><LoaderCircle className="animate-spin" size={16}/>Đang tải...</p> : tab === 'files' ? <div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{items.map((item) => <article key={item.id} className="rounded-xl border p-4"><a className="block truncate font-medium hover:underline" href={item.url} target="_blank" rel="noreferrer">{item.name}</a><p className="mt-1 text-xs text-muted-foreground">{item.mimeType} · {size(item.sizeBytes)} · {new Date(item.createdAt).toLocaleDateString('vi-VN')}</p><div className="mt-4 flex justify-between"><span className="text-xs text-muted-foreground">{item.source === 'generated' ? 'AI tạo' : 'Đã tải lên'}</span><Button size="icon-sm" variant="ghost" className="text-destructive" onClick={() => void request<void>({ url: `/library/assets/${item.id}`, method: 'DELETE' }).then(load)} aria-label={`Xóa ${item.name}`}><Trash2/></Button></div></article>)}{!items.length ? <p className="col-span-full py-12 text-center text-sm text-muted-foreground">Chưa có file. Tải lên file đầu tiên của bạn.</p> : null}</div> : <div className="mt-5 space-y-2">{memories.map((item) => <article key={item.id} className="rounded-xl border p-4"><p className="text-xs text-muted-foreground">{item.chatTitle} · {new Date(item.createdAt).toLocaleDateString('vi-VN')}</p><p className="mt-1 text-sm">{item.content}</p><Button size="sm" variant="ghost" className="mt-2 text-destructive" onClick={() => void request<void>({ url: `/memories/${item.id}`, method: 'DELETE' }).then(load)}><Trash2/> Xóa</Button></article>)}{!memories.length ? <p className="py-12 text-center text-sm text-muted-foreground">Chưa có memory phù hợp.</p> : null}</div>}<section className="mt-10 rounded-xl border border-dashed p-5"><h2 className="font-semibold">Nguồn kết nối</h2><p className="mt-1 text-sm text-muted-foreground">Google Drive, OneDrive/SharePoint và Dropbox đang cập nhật tính năng kết nối.</p></section></div>;
}
