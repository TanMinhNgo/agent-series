# ===== Thư viện cho project AI Agent =====
# Cài đặt (trong venv đã kích hoạt):  pip install -r requirements.txt

# --- Giao diện chat (UI) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# --- Tiện ích ---
python-dotenv>=1.0.0     # đọc biến môi trường từ file .env

# --- Nhà cung cấp LLM (chỉ cái bạn dùng mới thực sự cần key) ---
google-genai>=1.0.0      # Gemini — provider MẶC ĐỊNH của series
google-auth>=2.0.0       # xác minh ID token của Google Sign-In
anthropic>=0.40.0        # Claude — tuỳ chọn (cần key mới dùng được)
openai>=1.40.0           # GPT/Codex — tuỳ chọn (cần key mới dùng được)

# --- Cần cho tool read_pdf ---
pypdf>=4.0.0             # đọc nội dung file PDF

# --- Persistence + RAG cục bộ ---
SQLAlchemy>=2.0.0
alembic>=1.14.0
psycopg[binary]>=3.2.0   # PostgreSQL driver
pgvector>=0.3.0
sentence-transformers>=3.0.0
# Chỉ dùng splitter của LangChain; không kéo Agent/Chain/Loader vào kiến trúc lõi.
langchain-text-splitters>=0.3.0
pytest>=8.0.0
coverage>=7.0.0
python-docx>=1.1.2
openpyxl>=3.1.5
python-pptx>=1.0.2
reportlab>=4.2.0
cryptography>=42.0.0       # mã hóa refresh token của connector OAuth
imagekitio>=5.0.0          # lưu file private và signed URL qua ImageKit
